clc
clear all
% Else If Statement
% First Condition
x=5;
if(x>10)
    y1=log(x)
elseif (x>=0)
    y2=sqrt(x)
else(x<0)
    y3=exp(x)-1
end
% Second condition
x=16;
if(x>10)
    y1=log(x)
elseif (x>=0)
    y2=sqrt(x)
else(x<0)
    y3=exp(x)-1
end